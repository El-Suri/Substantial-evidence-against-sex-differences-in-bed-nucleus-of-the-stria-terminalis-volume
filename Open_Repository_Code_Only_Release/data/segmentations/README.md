# Segmentation placeholder

No BNST segmentation masks are included in this code-only release.

After HCP-approved mapped study IDs are available, masks should be named:

```text
<study_id>.nii.gz
```
