# Open-derived data placeholder

No open-derived participant-level table is included in this code-only release.

After HCP-approved mapped study IDs are available, add:

```text
bnst_open_derived.csv
```

using `study_id` as the join key.
